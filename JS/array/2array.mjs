let info = ["ram", 35, false]

console.log(info);

info[2]=true
info[0]="Hari"

//delete array element
delete info[1]
console.log(info);

//key point:
/* 
    - Arrays help organize multiple values under a single
    name.
    - Indexing starts at 0,which means the first
    element is at position 0.
    - we can easily access, modify, or add items to an 
    array.
*/
